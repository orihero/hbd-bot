# HBD Bot — copy to .env and fill in. Never commit .env.
# Every variable is HBD_-prefixed and maps 1:1 to a field on hbd.config.Settings.
# Startup fails fast and names any variable that is missing or out of range.

# ---------------------------------------------------------------------------
# Runtime
# ---------------------------------------------------------------------------
HBD_ENVIRONMENT=dev                       # dev | staging | prod
HBD_LOG_LEVEL=INFO                        # DEBUG | INFO | WARNING | ERROR
HBD_IS_DEBUG=false

# Swap EVERY vendor for its fake: no key is read, no request is made, nothing is
# spent. All-or-nothing on purpose — a half-fake run spends money for a result
# nobody can trust. Refused outright when HBD_ENVIRONMENT=prod.
HBD_USE_FAKE_PROVIDERS=false

# ---------------------------------------------------------------------------
# Telegram  (REQUIRED)
# ---------------------------------------------------------------------------
# from @BotFather
HBD_TELEGRAM_BOT_TOKEN=
# leave empty for long-polling in dev

# ---------------------------------------------------------------------------
# Infrastructure  (DATABASE_URL is REQUIRED)
# ---------------------------------------------------------------------------
HBD_DATABASE_URL=postgresql+asyncpg://hbd:hbd@localhost:5432/hbd
HBD_REDIS_URL=redis://localhost:6379/0

# ---------------------------------------------------------------------------
# ElevenLabs — Eleven Music, RU/EN TTS (v3) and Scribe v2 STT  (KEY REQUIRED)
# ---------------------------------------------------------------------------
# sent as the xi-api-key header
HBD_ELEVENLABS_API_KEY=
HBD_ELEVENLABS_BASE_URL=https://api.elevenlabs.io
HBD_MUSIC_MODEL_ID=music_v2               # music_v1 | music_v2 (v2 needed for inpainting)
HBD_MUSIC_OUTPUT_FORMAT=mp3_44100_128
HBD_MUSIC_MAX_CONCURRENCY=2               # 2 on Starter/Creator/Pro, 5 on Scale. A 429 above it.
HBD_MUSIC_USD_PER_MINUTE=0.15             # the rate the usage line's cost estimate derives from
HBD_TTS_MAX_CONCURRENCY=3
HBD_TTS_MODEL_ID=eleven_v3
HBD_STT_MODEL_ID=scribe_v2

# Leave at 0 to record billed characters without inventing a dollar figure.
HBD_ELEVENLABS_USD_PER_CHARACTER=0

# ---------------------------------------------------------------------------
# The TTS cast and routing — both are DATA, replaceable without a deploy.
# Leave empty for the shipped catalogue (3 characters x 4 languages) and the
# shipped routing (all four languages -> ElevenLabs, the only TTS vendor).
# A malformed override fails startup; it never silently restores the default.
# Adding a second vendor means supplying its voice ids here AND its route below;
# the id and the route travel together.
# ---------------------------------------------------------------------------
# a JSON array of voice entries
HBD_TTS_VOICE_REGISTRY_JSON=
# e.g. ru=elevenlabs_tts
HBD_TTS_ROUTES=

# ---------------------------------------------------------------------------
# LLM — intake normalisation, lyric writing, name respelling  (KEY REQUIRED)
# Gemini 3.7 Flash on the PAID tier. The free tier trains on your data and this
# payload contains a third party's name and private family facts.
# ---------------------------------------------------------------------------
# OpenRouter. It is OpenAI-compatible, so PROVIDER stays 'openai' and only the base URL
# changes. Swapping models is a one-line edit — no code change, no redeploy.
# sk-or-v1-...
HBD_LLM_API_KEY=
HBD_LLM_BASE_URL=https://openrouter.ai/api/v1
HBD_LLM_MODEL_ID=google/gemma-4-31b-it:free
HBD_LLM_PROVIDER=openai                   # gemini | openai — DECLARED, never inferred from the id
#
# Free models that support response_format (REQUIRED — the lyric call is strict JSON):
#   google/gemma-4-31b-it:free            Google lineage; best Uzbek prior of the free set
#   google/gemma-4-26b-a4b-it:free        smaller, faster
#   z-ai/glm-5.2:free                     256k ctx
#   minimax/minimax-m3:free               1M ctx
# Free models WITHOUT response_format (nemotron-3.5-lightning, inkling, laguna, ...) will
# fall through to the repair-and-revalidate path on every call. Avoid them.
#
# blank = no fallback
HBD_LLM_FALLBACK_API_KEY=
HBD_LLM_FALLBACK_PROVIDER=openai          # gemini | openai
HBD_LLM_FALLBACK_BASE_URL=https://openrouter.ai/api/v1
HBD_LLM_FALLBACK_MODEL_ID=z-ai/glm-5.2:free
HBD_LLM_TEMPERATURE=0.7
HBD_LLM_MAX_OUTPUT_TOKENS=2048

# ---------------------------------------------------------------------------
# Object storage (S3-compatible; R2 preferred for zero egress)
# ---------------------------------------------------------------------------
# No S3 client ships in this build: hbd.storage.LocalFileStorage archives finished
# assets under ./var/archive. The settings above are read by the S3 backend that
# replaces it behind the same protocol.

# ---------------------------------------------------------------------------
# Timeouts, seconds
# ---------------------------------------------------------------------------
HBD_LLM_TIMEOUT_S=45
HBD_MUSIC_TIMEOUT_S=420
HBD_TTS_TIMEOUT_S=60
HBD_STT_TIMEOUT_S=30

# ---------------------------------------------------------------------------
# Retry bounds
# ---------------------------------------------------------------------------
HBD_PROVIDER_MAX_ATTEMPTS=4
HBD_PROVIDER_BACKOFF_BASE_S=5
HBD_PROVIDER_BACKOFF_JITTER=0.2
HBD_LLM_PARSE_MAX_ATTEMPTS=2              # re-prompts after a failed JSON repair

# ---------------------------------------------------------------------------
# THE NAME SUBSYSTEM — the product's differentiator
#
# HBD_NAME_CANDIDATE_ORDER is the ranked list of candidate orthographies, tried
# left to right. IT IS CONFIGURATION, NOT CODE: a bake-off result is applied by
# reordering this line. Valid values:
#   stripped   — U+02BB modifier letters removed entirely   (Gulomjon)
#   canonical  — U+02BB preserved                           (Gʻulomjon)
#   hyphenated — syllable boundaries                        (Gu-lom-jon)
#   ascii      — U+0027 apostrophe                          (G'ulomjon)
#   phonetic   — respelling aimed at the vendor's grapheme model
#   cyrillic   — Cyrillic transliteration                   (Ғуломжон)
# Comma-separated, no duplicates.
# ---------------------------------------------------------------------------
HBD_NAME_CANDIDATE_ORDER=stripped,canonical,hyphenated,ascii,phonetic
HBD_NAME_VERIFICATION_MAX_ATTEMPTS=3      # re-rolls before we deliver the best attempt
HBD_NAME_MATCH_MIN_SIMILARITY=0.85        # STT transcript vs intended name, post-normalisation
HBD_NAME_CHUNK_DURATION_MS=8000           # the name gets its own chunk (3000-120000)
HBD_IS_NAME_VERIFICATION_ENABLED=true

# ---------------------------------------------------------------------------
# Song and greeting shape
# ---------------------------------------------------------------------------
HBD_SONG_LENGTH_MS=120000                 # 3000-600000
HBD_SONG_BODY_CHUNK_DURATION_MS=20000     # 3000-120000, max 30 chunks per plan
HBD_GREETING_MIN_DURATION_S=20
HBD_GREETING_MAX_DURATION_S=45
HBD_GREETINGS_PER_KIT=3

# ---------------------------------------------------------------------------
# Audio post — ffmpeg. Telegram sendVoice REQUIRES OGG/Opus or it renders as a
# file attachment instead of a waveform bubble.
# ---------------------------------------------------------------------------
HBD_FFMPEG_BINARY=ffmpeg
HBD_FFPROBE_BINARY=ffprobe
HBD_FFMPEG_TIMEOUT_S=180                  # subprocess wall clock; not a vendor timeout
HBD_LOUDNORM_SONG_LUFS=-14
HBD_LOUDNORM_SPEECH_LUFS=-16
HBD_LOUDNORM_TRUE_PEAK_DB=-1
HBD_OPUS_BITRATE_BPS=32000
HBD_OPUS_SAMPLE_RATE_HZ=48000
HBD_FADE_IN_MS=250
HBD_FADE_OUT_MS=1500
HBD_SILENCE_TRIM_THRESHOLD_DB=-50

# ---------------------------------------------------------------------------
# Moderation and pricing
# ---------------------------------------------------------------------------
HBD_IS_MODERATION_ENABLED=true
HBD_KIT_PRICE_AMOUNT_MINOR=0              # payment is out of scope; 0 until a rail lands
HBD_KIT_CURRENCY=UZS

# ---------------------------------------------------------------------------
# Retention, in days. The purge job reads these; the defaults are the ones the
# privacy commitments require, so shortening them is a policy decision.
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Languages. Interface and output language are chosen INDEPENDENTLY.
# ---------------------------------------------------------------------------
HBD_DEFAULT_UI_LANGUAGE=uz_latn           # uz_latn | uz_cyrl | ru | en
HBD_SUPPORTED_LANGUAGES=uz_latn,uz_cyrl,ru,en

# ---------------------------------------------------------------------------
# Free tier — text and one short spoken clip, never music
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Queue
# ---------------------------------------------------------------------------
HBD_WORKER_CONCURRENCY=15                 # keep below the provider ceiling
HBD_QUEUE_JOB_TIMEOUT_S=900
HBD_QUEUE_RESULT_TTL_S=3600            # how long a job result lingers in Redis

# ---------------------------------------------------------------------------
# Payment is OUT OF SCOPE for this build. NoopPaymentProvider always authorises
# and needs no configuration. No rail credentials belong in this file.
# ---------------------------------------------------------------------------
