# DELETE /v1/voice-changer/history/{id}
# Delete voice changer task

method: DELETE
path: /v1/voice-changer/history/{id}
group: Voice Changer
title: Delete task
description: Delete a voice changer task and its associated audio files (source and result)
order: 5

# Authentication
auth:
  type: xi-api-key
  required: true

# Path Parameters
path_parameters:
  - name: id
    type: string
    required: true
    description: Task ID to delete

# Query Parameters
query_parameters: []

# Request Body
request_body: null

# Code Examples
examples:
  curl: |
    curl -X DELETE "https://api.vibi.pro/v1/voice-changer/history/{id}" \
      -H "xi-api-key: YOUR_API_KEY"

  javascript: |
    const response = await fetch('https://api.vibi.pro/v1/voice-changer/history/{id}', {
      method: 'DELETE',
      headers: {
        'xi-api-key': 'YOUR_API_KEY'
      }
    });
    const data = await response.json();

  python: |
    import requests

    response = requests.delete(
        'https://api.vibi.pro/v1/voice-changer/history/{id}',
        headers={'xi-api-key': 'YOUR_API_KEY'},
    )
    data = response.json()

# Responses
responses:
  - status: 200
    description: OK
    example: |
      {
        "message": "deleted"
      }
