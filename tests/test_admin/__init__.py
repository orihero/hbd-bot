"""Tests for the admin panel package.

A package rather than a bare directory because ``tests/`` is type-checked under
``mypy --strict`` alongside ``src/``, and every other test directory here is one.
"""
